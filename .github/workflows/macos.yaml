name: macos

on:
  workflow_dispatch:
    inputs:
      version:
        description: Build version
        required: True

jobs:
  build-macos:
    name: MacOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.71.0
      - name: Install cargo bundle
        run: cargo install cargo-bundle
      - name: cargo bundle
        run: cargo bundle --release
      - name: Download butler
        run: wget -O butler.zip https://broth.itch.ovh/butler/darwin-amd64/LATEST/archive/default
      - name: Unzip butler
        run: unzip butler.zip
      - run: chmod +x butler
      - name: butler login
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }}
        run: ./butler login
      - run: cd target/release && ls && cd ../..
      - name: butler push
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }}
        run: ./butler push target/release/bundle/Cacophony.app subalterngames/cacophony:macos --userversion=${{ inputs.version }}

